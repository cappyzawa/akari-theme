name: Publish tmux Theme

on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Generate GitHub App token
      uses: actions/create-github-app-token@v1
      id: app-token
      with:
        app-id: ${{ secrets.AKARI_APP_ID }}
        private-key: ${{ secrets.AKARI_APP_PRIVATE_KEY }}
        repositories: akari-tmux

    - name: Checkout akari-tmux
      uses: actions/checkout@v4
      with:
        repository: cappyzawa/akari-tmux
        token: ${{ steps.app-token.outputs.token }}
        path: akari-tmux

    - name: Sync tmux directory
      run: |
        rsync -av --delete --exclude='.git' tmux/ akari-tmux/

    - name: Commit and push
      working-directory: akari-tmux
      run: |
        git config user.name "akari-theme[bot]"
        git config user.email "akari-theme[bot]@users.noreply.github.com"
        git add -A
        if git diff --staged --quiet; then
          echo "No changes to commit"
        else
          git commit -m "Sync from akari-theme ${{ github.ref_name }}"
          git push
        fi
